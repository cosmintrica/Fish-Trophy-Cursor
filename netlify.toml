[build]
  base = "."
  command = "cd client && npm ci && npm run build"
  publish = "client/dist"

[build.environment]
  NODE_VERSION = "20"
  DEBUG = "vite:*"
  SECRETS_SCAN_OMIT_PATHS = "env.example,dist/**/*,node_modules/**/*,client/node_modules/**/*,netlify/functions/**/*,.env*"
  SECRETS_SCAN_ENABLED = "false"
  CI = "false"
  
  # Supabase Configuration
  VITE_SUPABASE_URL = "https://cckytfxrigzkpfkrrqbv.supabase.co"
  VITE_SUPABASE_ANON_KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImNja3l0ZnhyaWd6a3Bma3JycWJ2Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTY4NDE4MDgsImV4cCI6MjA3MjQxNzgwOH0.-QGnkH6omx0V1FNZrbKA2LNI90ZMe6RRAa5ct25U65M"
  VITE_ADMIN_EMAIL = "cosmin.trica@outlook.com"
  
  # Map Configuration (MapLibre doesn't need token)
  VITE_MAP_TILES_URL = "https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"
  VITE_MAP_ATTRIBUTION = "© OpenStreetMap contributors"
  
  # API Configuration
  VITE_API_BASE_URL = "http://localhost:3001/api"
  
  # Subscription Configuration
  VITE_SUBSCRIPTION_ENABLED = "true"
  VITE_PREMIUM_FEATURES_ENABLED = "true"

# Cache optimization - using built-in Netlify cache

[dev]
  command = "npm run dev:client"
  port = 8889
  targetPort = 5173

# API redirects - must come BEFORE catch-all redirect
# Order is CRITICAL - most specific routes first!

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
  force = true

# Catch-all redirect for SPA - must come LAST
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions]
  directory = "netlify/functions"

# Supabase extension configured via Netlify App UI - no plugin configuration needed
# Trigger rebuild