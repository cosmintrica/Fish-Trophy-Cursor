// Re-export everything from the global theme context
export * from '../../contexts/ThemeContext';
export { ThemeProvider as ForumThemeProvider } from '../../contexts/ThemeContext'; // Optional alias if needed
// The global ThemeProvider is now at the App root

