# Netlify redirects file
# This file takes precedence over netlify.toml redirects
# IMPORTANT: In development, Netlify Dev proxies Vite routes automatically
# These redirects only apply in production

# API routes
/api/og.png  /.netlify/functions/og  200
/sitemap.xml  /.netlify/functions/sitemap  200
/api/*  /.netlify/functions/:splat  200

# Static assets (production only - in dev, Vite handles these)
/assets/*  /assets/:splat  200

# Catch-all for SPA - must be last
# Note: /src/*, /@vite/*, /@id/* are handled by Vite dev server in development
/*  /index.html  200

