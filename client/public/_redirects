# Netlify redirects file
# This file takes precedence over netlify.toml redirects

# Exclude Vite dev server routes from SPA redirect in development
# These routes should be proxied to Vite dev server (port 5173)
/src/*  /src/:splat  200
/assets/*  /assets/:splat  200
/node_modules/*  /node_modules/:splat  200
/@vite/*  /@vite/:splat  200
/@id/*  /@id/:splat  200

# API routes
/api/og.png  /.netlify/functions/og  200
/sitemap.xml  /.netlify/functions/sitemap  200
/api/*  /.netlify/functions/:splat  200

# Catch-all for SPA - must be last
/*  /index.html  200

